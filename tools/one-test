#!/bin/bash

ulimit -d 1200000
ulimit -t 3000

SELFDIR=`dirname $0`
TEST_DRIVER="${SELFDIR}/test-driver"

/usr/bin/time -o $1.time ${TEST_DRIVER} '(read-and-try-expectation!)' < $1.expect > $1.fail

if [ -s $1.fail ]
then
    echo "FAIL" $1
else
    echo "ok" $1
fi
