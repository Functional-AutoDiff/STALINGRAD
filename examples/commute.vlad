(include "math")

(list (let ((f ((*j (j* sqrt)) (cons (cons 4 1) identity))))
       (list (car (car f))
	     (cdr (car f))
	     (car ((cdr f) 1 1))
	     (cdr ((cdr f) 1 1))))
      (let ((f ((j* (*j sqrt)) (cons (cons 4 identity) (cons 1 '())))))
       (list (car (car f))
	     (car ((cdr (car f)) 1 1))
	     (cdr ((cdr (car f)) 1 1))
	     (car (cdr f)))))
