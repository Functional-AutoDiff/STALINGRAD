(include "math")

(list (let ((f ((conjoint-*j (conjoint-j* sqrt))
		(cons (cons 4 identity) (cons 1 identity)))))
       (list (car (car f))
	     (car (cdr f))
	     ((cdr (car f)) 1)
	     ((cdr (cdr f)) 1)))
      (let ((f ((conjoint-j* (conjoint-*j sqrt))
		(cons (cons 4 1) identity))))
       (list (car (car f))
	     (cdr (car f))
	     (car ((cdr f) (cons 1 1)))
	     (cdr ((cdr f) (cons 1 1))))))
