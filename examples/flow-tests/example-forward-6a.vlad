(define (derivative-using-j*-a f)
 (let ((f-forward (j* f))) (lambda (x) (tangent (f-forward (bundle x 1))))))

(define (derivative-using-j*-b f)
 (let ((f-forward (j* f))) (lambda (x) (tangent (f-forward (bundle x 1))))))

((derivative-using-j*-a (derivative-using-j*-b sqrt)) .25)
