(include "j")

(define (identity x) x)

(define (derivative-using-*j f)
 (let ((f-*j (*j f))) (lambda (x) (cdr ((cdr (f-*j x)) 1)))))

;;; nonconformance

((derivative-using-*j (derivative-using-*j (lambda (x) (identity x)))) 5)
